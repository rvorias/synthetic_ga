# Synthetic Loot Botting

This code is based on https://github.com/Anish-Agnihotri/dhof-loot.

## Run locally

```bash
# Install dependencies (in root folder)
npm install

# Start botting
python3 parallel.py [NUM_THREADS]
```
